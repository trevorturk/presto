# gems
gem install activerecord erubis sinatra

# see also
http://heroku.com
http://github.com/rtomayko/shotgun
http://github.com/sickill/racksh

# get remote mysql db locally
mysqldump -u USERNAME -p -h HOST DATABASE > dumpfile
mysql -u USERNAME -p DATABASE < dumpfile

# todo
pagination
comments on single post page
rss feed
make tt heroku app
move ae db to tt heroku app
redirect ae->tt w/ rack app (make ae heroku app)
no-www for ae & tt

# later
projects in sidebar
deny access to erubis/rb files in public (or move themes out of public)
make gem for routes and/or wp models (with tests)